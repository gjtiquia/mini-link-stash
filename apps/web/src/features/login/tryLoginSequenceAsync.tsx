// import { useAuthStore } from "@/store";
// import { router } from "@/providers/RouterProvider/router";

// TODO : deprecate. Should be an <a> call to the server to be redirected.
// TODO : When redirecting back... server should redirect client on callback to homepage
export async function tryLoginSequenceAsync() {
    // useAuthStore.getState().login();
    // router.navigate({ to: "/dashboard" });
}
